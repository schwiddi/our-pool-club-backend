import instanceSocket from './instanceSocket';
import jobSocket from './jobSocket';
import nodeSocket from './nodeSocket';

const sockets = [
  instanceSocket,
  jobSocket,
  nodeSocket,
];

export default sockets;
